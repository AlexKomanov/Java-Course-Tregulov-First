package lesson_7;

/**
 * @author Alex Komanov
 */
public class AccessModifiers {

    /**
     * 4 access modifiers
     * @public -> seen in all packages/classes
     * @private -> seen only in the class
     * @protected -> seen for the classes in the same package + seen for the children of the class (even in the different packages)
     * @package_default -> seen for the classes in the same package. Not seen in the different packages.
     *
     */
}
